//
//  ContentView.swift
//  StoriesApp
//

import SwiftUI
import StoriesAppModels

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}
